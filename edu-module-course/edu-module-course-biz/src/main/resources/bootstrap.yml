server:
    port: 40000

spring:
    application:
        name: edu-module-course

    profiles:
        active: local

logging:
    file:
        name: ${user.home}/logs/${spring.application.name}.log
