server:
    port: 40000

spring:
    application:
        name: edu-module-course

    profiles:
        active: ${profile.name}

logging:
    file:
        name: /logs/${spring.application.name}.log
