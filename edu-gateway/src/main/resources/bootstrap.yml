server:
    port: 48080

spring:
    application:
        name: edu-gateway-server

    profiles:
        active: ${profile.name}

logging:
    file:
        name: /logs/${spring.application.name}.log




